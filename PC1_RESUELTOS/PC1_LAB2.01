La práctica calificada 1 de Programación III requiere la implementación de un sistema de combate genérico para un
videojuego de rol en C++. Este sistema debe incluir diferentes personajes (Guerrero, Mago y Arquero) que interactúan con
 objetos y realizan acciones como atacar, defenderse y usar habilidades. Los principales puntos a evaluar son:

Clases y Objetos: Se debe crear una clase base abstracta Personaje con atributos esenciales como salud, ataque, defensa,
mana, etc., y métodos virtuales puros para atacar, defenderse y usar habilidades. Las clases derivadas (Guerrero, Mago,
Arquero) deben implementar estos métodos con lógicas específicas para cada tipo de personaje.

Sobrecarga de Operadores: Se debe sobrecargar el operador + para permitir que los objetos de tipo Personaje sumen el
poder de los objetos (como armas) a los atributos del personaje.

Funciones Template y Variadic Templates: Implementar una función aplicarObjeto usando funciones template para aplicar
diferentes objetos que modifiquen atributos de los personajes. Además, se debe permitir que un personaje realice múltiples
acciones en un solo turno utilizando variadic templates.

El desarrollo debe ser completo, con énfasis en la correcta aplicación de herencia, polimorfismo, plantillas de funciones
y manejo de variadic templates. Además, se debe comentar adecuadamente cada parte del código.